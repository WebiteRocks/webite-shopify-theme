<style>
/* Critical CSS - Prevents layout shift before UIkit loads */

/* Box sizing */
*, *::before, *::after { box-sizing: border-box; }

/* Base document */
html { height: 100%; }
body { margin: 0; width: 100%; min-height: 100%; }

/* Hidden utility */
[hidden] { display: none !important; }

/* Visibility - Responsive (matches UIkit breakpoints) */
@media (max-width: 959px) {
  .uk-visible\@m { display: none !important; }
}
@media (min-width: 960px) {
  .uk-hidden\@m { display: none !important; }
}
@media (max-width: 639px) {
  .uk-visible\@s { display: none !important; }
}
@media (min-width: 640px) {
  .uk-hidden\@s { display: none !important; }
}

/* Container - matches UIkit exactly */
.uk-container {
  display: flow-root;
  box-sizing: content-box;
  max-width: var(--container-max-width, 1200px);
  margin-left: auto;
  margin-right: auto;
  padding-left: 15px;
  padding-right: 15px;
}
@media (min-width: 640px) {
  .uk-container { padding-left: 30px; padding-right: 30px; }
}
@media (min-width: 960px) {
  .uk-container { padding-left: 40px; padding-right: 40px; }
}

/* Flexbox */
.uk-flex { display: flex; }
.uk-flex-inline { display: inline-flex; }
.uk-flex-row { flex-direction: row; }
.uk-flex-column { flex-direction: column; }
.uk-flex-wrap { flex-wrap: wrap; }
.uk-flex-nowrap { flex-wrap: nowrap; }
.uk-flex-center { justify-content: center; }
.uk-flex-between { justify-content: space-between; }
.uk-flex-around { justify-content: space-around; }
.uk-flex-left { justify-content: flex-start; }
.uk-flex-right { justify-content: flex-end; }
.uk-flex-top { align-items: flex-start; }
.uk-flex-middle { align-items: center; }
.uk-flex-bottom { align-items: flex-end; }
.uk-flex-stretch { align-items: stretch; }
.uk-flex-1 { flex: 1; }

/* Inline */
.uk-inline { display: inline-block; position: relative; max-width: 100%; vertical-align: middle; }

/* Navbar skeleton */
.uk-navbar-container { display: block; position: relative; }
.uk-navbar { display: flex; position: relative; align-items: center; min-height: 80px; width: 100%; }
.uk-navbar-left,
.uk-navbar-right,
[class*="uk-navbar-center"] { display: flex; align-items: center; gap: 15px; }
.uk-navbar-left { margin-right: auto; }
.uk-navbar-right { margin-left: auto; }
.uk-navbar-nav { display: flex; margin: 0; padding: 0; list-style: none; }
.uk-navbar-nav > li { display: flex; position: relative; }
.uk-navbar-nav > li > a { display: flex; align-items: center; padding: 0 15px; min-height: 80px; }
.uk-navbar-item { display: flex; align-items: center; padding: 0 15px; min-height: 80px; }
.uk-navbar-toggle { display: flex; align-items: center; padding: 0 15px; min-height: 80px; }

/* Grid skeleton */
.uk-grid, [uk-grid] { display: flex; flex-wrap: wrap; margin: 0; padding: 0; list-style: none; margin-left: -30px; }
.uk-grid > *, [uk-grid] > * { box-sizing: border-box; max-width: 100%; padding-left: 30px; margin-top: 30px; }
.uk-grid > .uk-first-column, [uk-grid] > .uk-first-column { margin-top: 0; }
.uk-grid > *:first-child, [uk-grid] > *:first-child { margin-top: 0; }
.uk-grid-medium { margin-left: -30px; }
.uk-grid-medium > * { padding-left: 30px; }
[class*="uk-child-width"] > * { box-sizing: border-box; }
.uk-child-width-1-2 > * { width: 50%; }
.uk-child-width-1-3 > * { width: calc(100% / 3); }
.uk-child-width-1-4 > * { width: 25%; }
.uk-child-width-1-5 > * { width: 20%; }
.uk-child-width-1-6 > * { width: calc(100% / 6); }
@media (min-width: 640px) {
  .uk-child-width-1-2\@s > * { width: 50%; }
  .uk-child-width-1-3\@s > * { width: calc(100% / 3); }
  .uk-child-width-1-4\@s > * { width: 25%; }
}
@media (min-width: 960px) {
  .uk-child-width-1-2\@m > * { width: 50%; }
  .uk-child-width-1-3\@m > * { width: calc(100% / 3); }
  .uk-child-width-1-4\@m > * { width: 25%; }
  .uk-child-width-1-5\@m > * { width: 20%; }
  .uk-child-width-1-6\@m > * { width: calc(100% / 6); }
}

/* Card skeleton */
.uk-card { position: relative; box-sizing: border-box; }
.uk-card-default { background: #fff; }
.uk-card-body { padding: 30px; }
.uk-card-body.uk-padding-small { padding: 15px; }
.uk-card-media-top { overflow: hidden; }
.uk-card-title { font-size: 1.5rem; line-height: 1.4; }

/* Images - prevent layout shift */
img { max-width: 100%; height: auto; }

/* Collection grid images - reserve space */
.main-collection__image { object-fit: cover; width: 100%; height: 100%; position: absolute; top: 0; left: 0; }
.main-collection__no-image { display: flex; align-items: center; justify-content: center; background: #f5f5f5; position: absolute; top: 0; left: 0; width: 100%; height: 100%; }

/* Width utilities */
.uk-width-1-1 { width: 100%; }
.uk-width-1-2 { width: 50%; }
.uk-width-1-3 { width: calc(100% / 3); }
.uk-width-2-3 { width: calc(200% / 3); }
.uk-width-1-4 { width: 25%; }
.uk-width-3-4 { width: 75%; }
.uk-width-expand { flex: 1; min-width: 1px; }
.uk-width-auto { width: auto; }
@media (min-width: 960px) {
  .uk-width-1-4\@m { width: 25%; }
  .uk-width-3-4\@m { width: 75%; }
}

/* Accordion - hide content until UIkit initializes */
[uk-accordion] > li:not(.uk-open) > .uk-accordion-content { display: none; }
[uk-accordion] > li > .uk-accordion-content { overflow: hidden; }

/* Section skeleton */
.uk-section { display: flow-root; box-sizing: border-box; padding-top: 40px; padding-bottom: 40px; }
@media (min-width: 960px) {
  .uk-section { padding-top: 70px; padding-bottom: 70px; }
}

/* Images - prevent layout shift */
img { max-width: 100%; height: auto; }
.main-collection__image { object-fit: cover; width: 100%; height: 100%; position: absolute; top: 0; left: 0; }
.main-collection__no-image { display: flex; align-items: center; justify-content: center; background: #f5f5f5; position: absolute; top: 0; left: 0; width: 100%; height: 100%; }

/* Position */
.uk-position-relative { position: relative; }

/* Text alignment */
.uk-text-center { text-align: center; }
.uk-text-left { text-align: left; }
.uk-text-right { text-align: right; }

/* Display */
.uk-display-block { display: block; }
.uk-display-inline-block { display: inline-block; }

/* Dropdowns/Drops - hidden by default */
.uk-dropdown,
.uk-drop,
.uk-navbar-dropdown,
[uk-drop],
[uk-dropdown],
[uk-navbar-dropdown] { display: none; }

/* Offcanvas - hidden by default */
.uk-offcanvas { display: none; }
</style>
